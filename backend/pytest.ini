[pytest]
# pytest配置文件
# 忽略agents/test_agent.py文件（这是一个独立的测试脚本，不是pytest测试）
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 忽略agents/test_agent.py
norecursedirs = agents/test_agent.py

# 或者使用testpaths来指定测试目录（不包含agents目录）
# testpaths = tests

# 忽略特定文件模式（更精确的方式）
# 注意：pytest.ini在backend目录，所以需要相对于backend的路径
